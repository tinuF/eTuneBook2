import { Route } from '@angular/router';
import { SetListComponent } from './set-list.component';

export const SetListRoutes: Route[] = [
  {
    path: 'sets',
    component: SetListComponent
  }
];
