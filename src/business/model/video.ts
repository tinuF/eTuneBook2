export class Video {
  source: string;
  code: string;
  description: string;

  constructor(source, code, description) {
    this.source = source;
    this.code =  code;
    this.description = description;
  }
}
